# /agentic-mutation-check — shortcut for the agentic-mutation-check skill

1. Read `.cline/skills/agentic-mutation-check/SKILL.md` (fallback `~/.cline/skills/agentic-mutation-check/SKILL.md`). If it does not exist, tell the user the skill is not installed and STOP.
2. Follow it exactly, Steps 1–5. Its RULES override anything else.
3. Inline arguments (fixed order, each one skips its question): `/agentic-mutation-check.md [score% | report-only]`.
